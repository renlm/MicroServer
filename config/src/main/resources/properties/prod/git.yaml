spring:
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/renlm/MicroServer.git
          forcePull: true
          cloneOnStart: false
          defaultLabel: master
          search-paths: repo/config/{profile}