apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "coreservice.fullname" . }}-test-connection"
  labels:
    {{- include "coreservice.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "coreservice.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
