apiVersion: v1
kind: Service
metadata:
  name: {{ include "coreservice.fullname" . }}
  labels:
    {{- include "coreservice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "coreservice.selectorLabels" . | nindent 4 }}
